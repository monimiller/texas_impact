[project]
authors = ["Edmund Miller <git@edmundmiller.dev>"]
channels = ["conda-forge", "bioconda"]
description = "Add a short description here"
name = "period-legislation"
platforms = ["osx-arm64"]
version = "0.1.0"

[tasks]
pipeline = "python rest_api_pipeline.py"

[dependencies]
dlt = ">=1.0.0,<2"
duckdb = ">=1.0.0,<2"
